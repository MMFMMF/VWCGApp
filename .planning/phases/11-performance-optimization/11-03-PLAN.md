---
phase: 11-performance-optimization
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User has verified Lighthouse mobile performance score meets >80 target"
    - "User has confirmed LCP, CLS metrics from Lighthouse report"
    - "User has reviewed bundle analysis report (dist/stats.html)"
    - "All 3 PRF requirements confirmed: PRF-03 (PageSpeed >80), PRF-04 (LCP <2.5s), PRF-05 (CLS <0.1)"
  artifacts: []
  key_links: []
---

<objective>
Human verification of performance metrics and Phase 11 completion

Purpose: User reviews the Lighthouse mobile report, confirms Core Web Vitals targets are met, and approves Phase 11 as complete. This checkpoint ensures the measurable performance requirements (PRF-03, PRF-04, PRF-05) are satisfied before declaring v1.1 milestone complete.

Output: User approval of performance metrics, Phase 11 marked complete.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-performance-optimization/11-01-SUMMARY.md
@.planning/phases/11-performance-optimization/11-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Performance optimization for the landing page including:
    - Self-hosted fonts (replaced Google Fonts CDN with @fontsource packages)
    - Astro build configuration (inlineStylesheets: 'auto', manualChunks vendor splitting)
    - Bundle analysis with rollup-plugin-visualizer
    - CLS audit of all v1.1 marketing components and React islands
    - Lighthouse mobile performance audit with documented metrics

    Requirements addressed:
    - PRF-03: Mobile PageSpeed score > 80
    - PRF-04: LCP < 2.5 seconds on mobile
    - PRF-05: CLS < 0.1 (no layout shift from animations)
  </what-built>
  <how-to-verify>
    1. **Review Lighthouse Report:** Open `lighthouse-report.html` in your browser
       - Confirm Performance score > 80
       - Confirm LCP < 2.5s
       - Confirm CLS < 0.1
       - Note any warnings or opportunities listed

    2. **Review Bundle Analysis:** Open `dist/stats.html` in your browser
       - Confirm total JavaScript bundle size
       - Verify < 100KB total increase from v1.0 baseline
       - Note largest chunks (react-vendor, charts, radix-ui)

    3. **Quick Visual Test:** Run `npm run preview` and visit http://localhost:4321
       - Confirm fonts render correctly (Inter body, Lexend headings)
       - Confirm no visible layout shifts on page load
       - Confirm animations still work smoothly (counters, gauges, expandable cards)
       - Check mobile view (DevTools responsive mode, 375px width)

    4. **Optional Online Validation:** Test deployed site on PageSpeed Insights
       - Visit https://pagespeed.web.dev/
       - Enter your Netlify deployment URL
       - Compare online results to local Lighthouse results

    5. **Checklist:**
       - [ ] PRF-03: Mobile PageSpeed score > 80
       - [ ] PRF-04: LCP < 2.5s on mobile
       - [ ] PRF-05: CLS < 0.1
       - [ ] Fonts render correctly (no missing glyphs or fallback fonts)
       - [ ] No visible layout shifts during page load or scroll
       - [ ] Bundle increase < 100KB from v1.0
  </how-to-verify>
  <resume-signal>Type "approved" if all metrics meet targets, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
User confirms all 3 PRF requirements are met:
1. PRF-03: Mobile PageSpeed score > 80 — user verified from Lighthouse report
2. PRF-04: LCP < 2.5s on mobile — user verified from Lighthouse report
3. PRF-05: CLS < 0.1 — user verified from Lighthouse report
</verification>

<success_criteria>
1. User has reviewed Lighthouse mobile report and confirmed Performance > 80
2. User has confirmed LCP < 2.5s and CLS < 0.1
3. User has visually verified fonts render correctly
4. User has confirmed no layout shifts during page load
5. All 3 PRF requirements marked as satisfied
6. Phase 11 approved as complete
</success_criteria>

<output>
After completion, create `.planning/phases/11-performance-optimization/11-03-SUMMARY.md`
</output>
